# Fluent Science Reading 产品需求文档

> 文档版本：V2.0  
> 产品阶段：可用型 MVP / 模块闭环建设  
> 更新日期：2026-08-23  
> 适用对象：产品、设计、前端、后端、内容教研、测试与运营  
> 需求基线：当前 `syllabus.html`、`index.html`、`server.js`、卡片 JSON 与数据库设计

## 1. 文档目的

本文档把现有英语科普阅读原型整理为可开发、可测试、可验收的产品规范，并明确本阶段的实施边界。

本次建设目标不是一次性覆盖完整商业化平台，而是先完成一条可靠的学生学习闭环，同时搭好账号、内容管理和数据统计的模块骨架。所有 P0 功能必须有明确入口、正常态、空态、加载态、错误态和完成反馈。

## 2. 产品定义

Fluent Science Reading 是面向小学阶段学习者的英语科普阅读 Web 应用。产品以“图片 + 结构化 JSON”阅读卡片为内容单元，将阅读、听读、词汇、阅读理解、结构重建和学习结果整合到同一学习路径中。

核心价值：

- 对学生：用短篇科普内容同时练习英语词汇、听力、阅读和科学思维。
- 对教师/家长：快速了解孩子学过什么、完成到哪里、哪些内容需要复习。
- 对内容团队：通过统一数据规范导入、校验、预览、发布和维护课程卡片。
- 对产品团队：为后续语音评分、个性化推荐、学习报告和内容规模化生产保留稳定接口。

## 3. 当前基础与差距

### 3.1 已具备

- 一个已发布课程系列和两张示例卡片。
- 课程系列与卡片目录接口。
- 卡片图片、正文、核心词、3Q 理解题和 Rebuild 数据渲染。
- 浏览器朗读、句子点读、语速选择和基础录音入口。
- 用户注册/登录、学习进度和录音元数据接口。
- JSON 文件存储、关系型数据库草案和卡片基础校验。

### 3.2 主要缺口

- 学习页尚未按 Reading、Listen、Words、Comprehension、Rebuild、Result 形成清晰模块。
- Word Match 和 Word in Context 数据存在但没有完整交互。
- 目录页“已完成”状态为固定演示逻辑，不读取用户真实进度。
- 缺少独立学习结果页、复习建议和模块得分汇总。
- 账号接口没有可使用的账号页面，登录态与进度用户未统一。
- 缺少内容管理后台界面、内容预览和发布工作台。
- 导航中存在无效按钮，错误态、空态、键盘操作和移动端体验不完整。
- 文件型存储适合当前演示环境；正式托管需迁移到平台持久化数据库，录音文件需进入对象存储。

## 4. 目标、指标与非目标

### 4.1 本阶段目标

1. 学生能从课程目录进入任意已发布卡片并完成六个学习模块。
2. 每次答题和模块完成状态自动保存，刷新后可以继续。
3. 课程目录展示真实学习完成度，并支持全部、未开始、学习中、已完成筛选。
4. 学生可在账号页注册、登录、查看汇总数据并退出。
5. 内容人员可在管理页查看系列和卡片、校验 JSON、创建草稿、发布或下架内容。
6. 页面在桌面、平板和手机宽度下可用，核心操作可通过键盘完成。

### 4.2 核心衡量指标

| 指标 | 定义 | MVP 目标 |
|---|---|---|
| 卡片开始率 | 进入学习页的用户数 / 查看目录的用户数 | 可被记录 |
| 卡片完成率 | 六个模块达到完成条件的卡片学习次数 / 开始次数 | 可被准确计算 |
| 模块完成率 | 每模块完成次数 / 进入该模块次数 | 可被准确计算 |
| 理解正确率 | Comprehension 正确题数 / 作答题数 | 结果页可见 |
| 词汇正确率 | Word Match 与 Context 正确数 / 总题数 | 结果页可见 |
| 继续学习成功率 | 刷新或重新打开后恢复到最近进度的成功率 | 100%（正常存储条件下） |

### 4.3 本阶段非目标

- 不实现真实 AI 发音评分，只保存录音提交及待处理状态。
- 不实现付费、订阅、优惠券或课程购买。
- 不实现独立家长端、班级管理和教师布置作业。
- 不实现社交、排行榜、徽章商城或复杂游戏化系统。
- 不实现内容 AI 自动生成与批量发布。

## 5. 用户与权限

### 5.1 游客

- 可浏览已发布课程、卡片列表和公开试学内容。
- 可进行本地试学；需要云端保存或查看账号数据时引导登录。

### 5.2 学生

- 浏览全部可访问的已发布内容。
- 完成各学习模块并保存个人进度、答案和录音提交。
- 查看个人结果、复习词汇和历史学习汇总。
- 只能读取和修改自己的学习数据。

### 5.3 内容编辑

- 查看并编辑课程系列和卡片草稿。
- 校验、预览、发布、下架内容。
- 不可管理管理员账号或系统级配置。

### 5.4 管理员

- 拥有内容编辑全部权限。
- 可查看聚合学习数据、用户与录音处理状态。
- 可调整账号角色和内容状态。

## 6. 信息架构与路由

| 页面 | 路由 | 主要用户 | 目的 |
|---|---|---|---|
| 我的课程 | `/`、`/syllabus.html` | 全部 | 浏览系列、筛选卡片、继续学习 |
| 卡片学习 | `/lesson/:slug` | 学生 | 完成六模块学习闭环 |
| 核心词库 | `/words.html` | 学生 | 汇总、搜索和复习卡片词汇 |
| 学习账号 | `/account.html` | 学生 | 注册、登录、查看汇总、退出 |
| 内容工作台 | `/admin.html` | 编辑/管理员 | 内容校验、创建、发布和下架 |
| 健康检查 | `/api/health` | 系统 | 检查服务可用性 |

全站主导航保持一致，当前页提供视觉标识；无功能的入口不得以可点击按钮出现。

## 7. 核心学习流程

```text
课程目录
  → 选择已发布卡片
  → Reading 阅读正文
  → Listen & Read 听读与录音
  → Words 词义匹配 + 语境选择
  → Comprehension 3Q 理解
  → Rebuild 结构重建
  → Result 查看结果与复习建议
  → 返回目录 / 再学一次 / 下一课
```

模块可自由切换，不强制锁定顺序。系统记录各模块完成情况，并在模块导航和结果页展示状态。切换模块不能丢失当前答案。

## 8. 功能需求

### 8.1 FR-CAT 课程目录

| ID | 需求 | 优先级 | 验收要点 |
|---|---|---|---|
| FR-CAT-001 | 加载所有已发布且至少包含一张已发布卡片的系列 | P0 | 无内容时展示明确空态 |
| FR-CAT-002 | 切换系列并加载该系列卡片 | P0 | 卡片数量、标题和描述同步变化 |
| FR-CAT-003 | 展示卡片日序、等级、主题、标题、封面和学习进度 | P0 | 数据来自卡片和用户进度接口 |
| FR-CAT-004 | 支持全部、未开始、学习中、已完成筛选 | P0 | 筛选结果与真实完成度一致 |
| FR-CAT-005 | 卡片入口文案随状态显示“开始学习”“继续学习”“再次学习” | P0 | 点击进入正确 slug |
| FR-CAT-006 | 提供加载、空、失败和重试状态 | P0 | 接口失败不出现无限加载 |

### 8.2 FR-REA Reading

- FR-REA-001：显示标题、Big Question、等级、主题和 Article Structure。
- FR-REA-002：完整显示阅读图片，图片加载失败时提供文字替代与错误提示。
- FR-REA-003：按段落和句子渲染正文，不把图片仅作为装饰。
- FR-REA-004：正文核心词高亮；点击或键盘激活后打开词义卡。
- FR-REA-005：词义卡展示英文、中文释义/简明解释，并支持发音。
- FR-REA-006：点击句子可朗读该句并标记当前句。
- FR-REA-007：学生阅读或操作后可手动标记 Reading 完成。

完成条件：学生点击“完成阅读”或进入后完成一次正文朗读。

### 8.3 FR-LIS Listen & Read

- FR-LIS-001：支持全文播放、暂停/停止和当前句高亮。
- FR-LIS-002：支持 0.8x、1.0x、1.2x、1.5x 语速。
- FR-LIS-003：优先播放卡片提供的音频；缺少音频时使用浏览器语音合成并说明状态。
- FR-LIS-004：展示核心句，支持示范朗读。
- FR-LIS-005：支持开始录音、停止、重录、试听和提交。
- FR-LIS-006：录音权限拒绝、设备不支持和提交失败均需明确反馈。
- FR-LIS-007：提交记录包含用户、卡片、目标句、格式、大小、时间和处理状态。

完成条件：全文播放完成，或成功提交一次核心句录音。

### 8.4 FR-WOR Words

Words 模块由 Word Match、Word in Context 和 Core Word Bank 组成。

#### Word Match

- 同一卡片显示 5–6 个核心词。
- L1 匹配词与中文/简明意义；L2 匹配词与功能；L3 匹配词与概念或因果关系。
- MVP 使用“先选单词、再选释义”的点击匹配方式，同时兼容键盘；不依赖拖拽才能完成。
- 正确配对后锁定并显示成功状态；错误配对提示重试但不直接泄露全部答案。
- 支持重置本轮练习。

#### Word in Context

- L1、L2 固定 2 题，L3 固定 3 题。
- 每题固定 A/B/C 三个简短选项。
- 作答后显示对错与简短反馈，允许修改后重新检查。

完成条件：所有 Word Match 配对完成并完成全部 Context 题。

### 8.5 FR-COM Comprehension

- 固定三题，类型依次为 Whole、Connection、Transfer。
- 每题只使用 A/B/C，选项短小并符合目标年龄阅读水平。
- 点击选项后先记录选择；学生点击“检查答案”后统一判定。
- 判定后展示每题对错、正确答案和可理解的反馈。
- 支持“再试一次”，保留尝试次数和最近一次答案。

完成条件：三题全部作答并提交检查。

### 8.6 FR-REB Rebuild

- 根据五类 Article Structure 自动显示匹配任务：

| 文章结构 | 默认任务 |
|---|---|
| `Process/Life cycle` | 步骤排序 |
| `Feature-Function` | 特征—功能映射或关系排序 |
| `Cause-Effect` | 因果链 |
| `Compare-Contrast` | 对比映射 |
| `Fact/Explanation` | 中心事实—支持信息映射或点击填空 |

- 当前示例卡使用点击添加、点击移除和上下移动的可访问排序控件。
- 检查后显示正确/需调整，并指出首个需要调整的位置。
- 支持重置任务；不能只依赖颜色表达结果。

完成条件：提交一次完整结构；正确性单独记录。

### 8.7 FR-RES Result

- 展示卡片总完成度、已完成模块数和最近学习时间。
- 分模块展示完成状态、得分和继续入口。
- 展示 Comprehension、Words、Rebuild 的正确率。
- 汇总错误词和需要复习的题目。
- 提供“回到课程”“再学一次”和“继续下一课”操作。
- 未完成模块有明确提示，不把“已作答”误标为“答对”。

总完成度计算：六个模块完成数 ÷ 6，四舍五入为整数百分比。

### 8.8 FR-ACC 账号与个人学习

- 注册字段：邮箱、密码；登录字段：邮箱、密码。
- 成功后保存会话标识，并把后续进度写入当前用户。
- 账号页展示邮箱、已开始卡片数、已完成卡片数、平均完成度和最近学习记录。
- 支持退出；退出后回到游客/演示状态。
- 密码不以明文保存；服务端不返回密码摘要。
- 正式 Sites 环境使用平台身份与服务端用户标识，不信任客户端传入的用户 ID。

### 8.9 FR-WBK 核心词库

- 汇总全部已发布卡片的核心词并去重。
- 支持按关键词搜索，显示词、释义、来源主题和发音入口。
- 支持“回到原文”进入来源卡片。
- 加载失败或无匹配结果时展示可恢复状态。

### 8.10 FR-ADM 内容工作台

#### 系列管理

- 查看系列 ID、名称、状态、排序和卡片数量。
- 新建草稿系列；修改名称、描述、排序和状态。
- “删除”采用归档/下架，不进行不可恢复的物理删除。

#### 卡片管理

- 查看卡片标题、系列、Day、Level、结构和状态。
- 粘贴或导入 JSON 后先校验，再允许保存草稿。
- 支持预览、发布、下架。
- 发布前必须通过必填字段、结构类型、词汇数量、题目和答案校验。

#### 数据概览

- 展示系列数、卡片数、用户数、学习记录数、录音提交数。
- 列表数据为空时显示 0，不显示错误占位值。

### 8.11 FR-STA 通用状态与反馈

- 所有异步页面包含 loading、empty、error、success 四类状态。
- 破坏性或状态变更操作要求二次确认或提供清晰撤销路径。
- Toast 用于短反馈；重要错误同时在相关模块内显示，不能只靠短暂 Toast。
- 网络失败时保留用户尚未提交的页面输入，并提供重试按钮。

## 9. 内容与数据规范

### 9.1 内容层级

```text
Course（大课程）
  └─ Series（系列）
      └─ Topic（主题领域）
          └─ Theme（具体主题）
              └─ Card（学习卡片）
```

现阶段 `seriesId` 可与 `courseId` 相同，但数据层需保留两者以便后续扩展。

### 9.2 Card JSON 必填字段

```json
{
  "version": "1.0",
  "cardId": "day001-flower",
  "courseId": "science-reading",
  "seriesId": "science-reading",
  "topic": "Living Things",
  "theme": "Flowers",
  "day": 1,
  "level": "L1",
  "title": "FLOWER",
  "bigQuestion": "How do flowers bloom?",
  "image_file": "day001-flower.png",
  "articleStructure": "Process/Life cycle",
  "status": "published",
  "paragraphs": [],
  "word_bank": [],
  "wordModule": {},
  "comprehension": {},
  "rebuild": {},
  "listenRead": {}
}
```

### 9.3 发布校验规则

- `cardId` 为 2–80 位 slug 安全字符，且全局唯一。
- `day` 为正整数；`level` 仅允许 L1、L2、L3。
- `paragraphs` 至少一个非空段落。
- `word_bank` 必须包含 5–6 个不重复核心词。
- Article Structure 只允许 `Feature-Function`、`Cause-Effect`、`Process/Life cycle`、`Compare-Contrast`、`Fact/Explanation` 五个标准值；导入可兼容历史分隔符，但保存时必须标准化。
- Comprehension 必须恰好 3 题，类型为 Whole、Connection、Transfer。
- 所有选择题必须恰好包含 A、B、C，且答案存在于选项中。
- Rebuild 类型必须与 Article Structure 匹配，步骤/选项不少于 3 项。
- 发布内容的图片引用必须可访问；音频可为空，前端需提供降级方案。

## 10. 数据模型与持久化

核心实体：`users`、`series`、`cards`、`learning_progress`、`recording_submissions`。

`learning_progress` 至少包含：

- `user_id`、`card_id` 联合唯一键。
- `answers_json`：各题答案与检查结果。
- `module_status_json`：六模块完成状态、得分、尝试次数。
- `last_module`：最后访问模块。
- `completed_percent`：0–100 的派生快照。
- `updated_at`：最近保存时间。

持久化原则：

- 当前本地原型使用 `data/app-db.json` 作为开发数据源。
- 正式托管使用平台结构化持久化存储；进度不能只存在 localStorage。
- localStorage 只保存非权威 UI 偏好和本地会话提示。
- 录音二进制文件进入对象存储，数据库只保存所属用户、卡片、格式、大小、路径和处理状态。
- 每次写入需校验用户对记录的所有权。

## 11. API 契约

| 方法 | 路径 | 用途 | 关键响应 |
|---|---|---|---|
| GET | `/api/health` | 服务健康检查 | `{ok, service}` |
| GET | `/api/series` | 已发布系列列表 | `{series[]}` |
| POST | `/api/series` | 创建系列草稿 | `series` |
| PATCH | `/api/series/:id` | 更新系列 | `series` |
| DELETE | `/api/series/:id` | 归档系列 | `series` |
| GET | `/api/cards` | 卡片目录，可按 seriesId 过滤 | `{cards[]}` |
| GET | `/api/cards/:idOrSlug` | 获取卡片完整内容 | `card` |
| POST | `/api/cards/validate` | 校验卡片 JSON | `{valid, errors[]}` |
| POST | `/api/cards` | 保存卡片草稿 | `{id, status}` |
| POST | `/api/cards/:id/publish` | 发布卡片 | `{id, status}` |
| POST | `/api/cards/:id/archive` | 下架卡片 | `{id, status}` |
| GET | `/api/progress/:userId` | 用户进度 | `{cards}` |
| PUT | `/api/progress/:userId` | 合并保存卡片进度 | `cardProgress` |
| POST | `/api/recordings/:userId` | 创建录音提交 | `recording` |
| GET | `/api/dashboard` | 管理概览 | 聚合计数与内容列表 |

接口统一要求：JSON 响应；4xx 表示可预期业务错误；5xx 表示服务错误；错误体使用 `{error, details?}`；禁止在错误中返回密码摘要或服务端敏感路径。

## 12. 非功能需求

### 12.1 可访问性

- 页面语言、标题层级、表单标签和图片 alt 正确。
- 所有核心操作支持键盘；焦点样式清晰可见。
- 不仅依赖颜色表达选中、正确、错误和完成状态。
- 交互控件触控目标建议不小于 44×44 CSS 像素。
- 支持 `prefers-reduced-motion`，避免不必要动画。

### 12.2 响应式

- 设计基准：桌面 ≥ 1024px、平板 600–1023px、手机 < 600px。
- 手机端模块导航可横向滚动或换行，正文与练习保持单列。
- 不产生影响核心内容阅读的横向页面滚动。

### 12.3 性能

- 首屏只加载当前页面所需数据和图片。
- 课程目录图片启用懒加载；避免重复请求同一资源。
- 常规 API 在本地数据规模下 500ms 内返回；错误超时有反馈。

### 12.4 安全与隐私

- 所有输出进行 HTML 转义，避免把 JSON 内容直接拼接为可执行标记。
- 服务端校验请求体、枚举值和 ID；拒绝目录穿越。
- 正式环境使用安全密码/平台身份方案，服务端执行授权检查。
- 录音默认私有，不能被其他学生枚举或访问。
- 不在客户端或仓库中暴露密钥、令牌和真实学生隐私数据。

### 12.5 兼容与降级

- 支持当前主流桌面和移动浏览器。
- 浏览器不支持 SpeechSynthesis 或 MediaRecorder 时，页面仍可完成阅读和答题，并说明功能不可用。

## 13. 埋点与统计事件

| 事件 | 触发时机 | 关键属性 |
|---|---|---|
| `catalog_viewed` | 目录加载成功 | series_id |
| `lesson_started` | 首次进入卡片 | card_id, level |
| `module_opened` | 切换模块 | card_id, module |
| `module_completed` | 达到完成条件 | card_id, module, score |
| `answer_checked` | 提交检查 | card_id, module, correct_count |
| `recording_submitted` | 录音提交成功 | card_id, size, mime |
| `lesson_completed` | 六模块完成 | card_id, duration |

MVP 可先在学习进度中保留等价字段，不要求接入第三方分析平台。

## 14. 验收标准

### 14.1 学生闭环

1. 目录正确显示两个已发布示例卡片。
2. 进入任一卡片后六个模块均可访问，切换不丢答案。
3. Word Match、Context、3Q 和 Rebuild 可独立完成并判定。
4. 结果页的模块状态、得分和总完成度与实际操作一致。
5. 刷新学习页后，已保存答案和进度可恢复。
6. 返回目录后，卡片按钮与筛选依据真实完成度变化。

### 14.2 账号

1. 新邮箱可注册并进入登录状态。
2. 重复注册、错误密码和缺少字段有清晰错误。
3. 登录后进度归属当前用户；退出后不继续显示其个人数据。

### 14.3 内容工作台

1. 合法示例 JSON 校验通过，缺字段或题目答案错误时展示逐项问题。
2. 草稿卡片不会出现在学生目录。
3. 校验通过的草稿可发布；发布后可在目录和学习页访问。
4. 下架后从学生目录消失，但内容和历史进度仍保留。

### 14.4 质量

1. 健康检查、系列、卡片、进度和管理概览接口返回正确状态码。
2. 桌面与手机宽度下无阻塞布局问题。
3. Tab 键可以访问导航、模块、选项和主要操作。
4. 页面无未捕获脚本错误；接口失败时可重试。

## 15. 需求—模块—测试追踪

| 范围 | 需求组 | 实现入口 | 主要验证 |
|---|---|---|---|
| 课程目录 | FR-CAT | `syllabus.html` | 系列/卡片接口、真实进度筛选 |
| 学习闭环 | FR-REA/LIS/WOR/COM/REB/RES | `index.html` | 两张卡片全模块交互 |
| 账号 | FR-ACC | `account.html` + auth API | 注册、登录、退出、统计 |
| 词库 | FR-WBK | `words.html` | 汇总、搜索、发音、回原文 |
| 内容后台 | FR-ADM | `admin.html` + content API | 校验、保存、发布、下架 |
| 持久化 | 数据模型/API | `server.js`、`db/`、`data/` | 刷新恢复、数据隔离 |
| 通用质量 | FR-STA/NFR | 全站 | 错误态、键盘、响应式 |

## 16. 实施优先级与里程碑

### M1：学生学习闭环（P0）

- 统一全站导航与页面状态。
- 完成六模块学习页、真实进度和结果汇总。
- 完成课程目录真实状态和筛选。

### M2：账号、词库和内容工作台（P0/P1）

- 完成账号页面和用户进度归属。
- 完成核心词库。
- 完成内容概览、JSON 校验、草稿创建、发布和下架。

### M3：持久化与托管适配（P0）

- 保证本地数据兼容现有内容。
- 托管版本迁移结构化数据和录音对象存储。
- 完成正式构建、冒烟测试和发布。

### M4：后续增强（P2）

- AI 发音评分、个性化复习、教师/家长报告。
- 更丰富的结构图任务、内容批量工具和国际化。

## 17. 上线检查清单

- [ ] 需求追踪表中的 P0 项全部通过。
- [ ] 两张示例卡片全部模块完成一次。
- [ ] 账号、进度、录音和管理数据无越权读取。
- [ ] 所有无效导航已移除或接入真实页面。
- [ ] 标题、描述、图标和社交分享信息为产品内容。
- [ ] 桌面、平板、手机完成布局检查。
- [ ] 发布环境的结构化存储、对象存储和身份配置正确。
- [ ] 部署后健康检查、目录、卡片和进度写入正常。

## 18. 决策记录

- 模块允许自由切换，以降低儿童用户的流程阻塞。
- 答题“已完成”和“答对”分开记录，避免完成度失真。
- Rebuild 提供点击式排序作为拖拽的可访问替代。
- 当前浏览器朗读是音频缺失时的降级方案，不等同于正式教学音频。
- 本地 JSON 数据库仅用于开发；正式用户数据必须使用平台持久化存储。
- 内容删除采用归档，保护已产生的学习记录和内容版本链。
