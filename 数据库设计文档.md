# Fluent Science Reading 数据与持久化设计

> 版本：V2.0  
> 更新：2026-08-23  
> 关联需求：`英语教学Web需求文档.md` 第 9–11 节

## 1. 设计目标

数据库承载账号、会话、课程系列、卡片内容、学习进度和录音提交。卡片正文以结构化 JSON 保存，便于内容团队按统一规范导入；检索、权限、状态和统计字段保持结构化。

本地开发由 `data/app-db.json` 提供可直接运行的数据源。正式 Sites 环境应将结构化数据迁移至平台数据库，并把录音等二进制文件存入对象存储；浏览器存储不能成为学习进度的权威数据源。

## 2. 核心关系

```text
users 1 ─── N sessions
users 1 ─── N learning_progress N ─── 1 cards
users 1 ─── N recording_submissions N ─── 1 cards
series 1 ─── N cards
```

## 3. 表说明

| 表 | 用途 | 关键约束 |
|---|---|---|
| `users` | 学生、编辑、管理员账号 | email 唯一；密码只保存安全摘要 |
| `sessions` | 不透明登录会话 | token 只保存摘要；关联 user；可过期 |
| `series` | 课程系列 | status 为 draft/published/archived |
| `cards` | 卡片元数据与内容 JSON | status 控制草稿、发布与下架；保留版本 |
| `learning_progress` | 用户按卡片的学习进度 | user_id + card_id 联合主键 |
| `recording_submissions` | 核心句录音提交 | 文件元数据与处理状态；文件本体外置 |

完整关系型草案见 `db/schema.sql`。

## 4. 学习进度结构

`learning_progress` 使用结构化列保存索引和统计字段，使用 JSON 字段保存可扩展模块详情：

```json
{
  "cardId": "day001-flower",
  "lastModule": "words",
  "answers": {
    "context": { "0": "A" },
    "comprehension": { "0": "A", "1": "B", "2": "A" }
  },
  "matched": ["bud", "roots"],
  "rebuild": [0, 1, 2, 3, 4],
  "moduleStatus": {
    "reading": { "completed": true, "score": 100 },
    "listening": { "completed": false, "score": 0 },
    "words": { "completed": true, "score": 100 }
  },
  "completedPercent": 33,
  "updatedAt": "2026-08-23T00:00:00.000Z"
}
```

完成度由六模块完成状态派生，服务端将输入限制在 0–100。答题完成与答题正确分开记录。

## 5. 卡片目录与内容源

- `cards` 表存放 ID、slug、series、topic、theme、day、level、title、结构、图片、状态和更新时间。
- 本地原型中完整内容位于 `<cardId>.json`；启动和读取目录时同步元数据到 `app-db.json`。
- 正式数据库可将完整 JSON 写入 `content_json`，但仍需把系列、状态、等级和更新时间等查询字段单独存列。
- “删除”使用 archived 状态，不物理删除已产生学习记录的内容。

## 6. 会话与权限

- 登录成功后生成不可预测的会话 token；客户端只保存 token，服务端保存 token 映射或摘要。
- 读取/写入进度和录音时，服务端必须把会话用户与目标 user_id 对齐。
- `demo` 仅用于本地无账号试学，不得用于正式用户数据。
- 管理写接口在正式环境必须校验 editor/admin 角色。
- Sites 托管版本优先采用平台身份标头或 SIWC，由服务端直接取得稳定用户 ID。

## 7. 录音存储

数据库只保存：

- `id`、`user_id`、`card_id`、`target_sentence`
- `file_path`、`mime`、`size`
- `status`：uploaded / processing / scored / failed
- `score`、`feedback_json`、`created_at`

正式环境把实际音频写入对象存储，并使用不可枚举路径；下载前校验所属用户或管理权限。当前本地 MVP 只记录录音元数据，用于验证提交闭环。

## 8. 索引与查询

- `users.email` 唯一索引用于登录。
- `sessions.user_id` 用于会话清理与退出全部设备。
- `cards.status`、`cards.series_id` 用于学生目录。
- `learning_progress.user_id` 用于账号汇总和目录进度。
- 后续数据量增长时，可增加 `(card_id, updated_at)` 与录音 `(status, created_at)` 索引。

## 9. 迁移与兼容

1. 先创建表和索引，不删除旧 JSON 数据。
2. 将 `data/app-db.json` 中 users、series、cards、progress、recordings 逐类导入。
3. 旧版 `moduleStatus` 布尔值读取时兼容为 `{completed: boolean, score: 0}`。
4. 缺少 `completedPercent` 的旧记录根据模块状态重算。
5. 校验用户数、卡片数、进度数和录音数后再切换正式数据源。
6. 迁移脚本必须可重复执行，并以稳定主键防止重复写入。

## 10. 备份与保留

- 内容发布和下架保留更新时间与版本号。
- 学习记录不因卡片下架而删除。
- 用户删除请求需级联清理会话、进度和录音元数据，并按策略删除对象存储文件。
- 正式环境应配置数据库备份和对象生命周期策略；本地 JSON 文件不作为生产备份方案。
